image: node:22-alpine

pages:
  stage: deploy
  script:
    - npm ci
    - npm run build
    - mv dist public-pages
    - rm -rf public
    - mv public-pages public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
